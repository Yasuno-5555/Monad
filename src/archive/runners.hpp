#pragma once
#include <string>

// Runner Declarations
int run_two_asset(const std::string& config_path);   // Was main_two_asset.cpp
int run_experiment_tt(const std::string& config_path); // Was main_experiment_tt.cpp

// Future runners can be added here
// int run_belief_hank(const std::string& config_path);
// int run_crypto_hank(const std::string& config_path);
